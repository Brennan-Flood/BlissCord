development:
  adapter: async

test:
  adapter: async

production:
  adapter: redis
  # url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
    url: <%= ENV.fetch("REDIS_URL") { "redis://discord-clone-1.herokuapp.com/" } %>

  channel_prefix: DiscordClone_production
